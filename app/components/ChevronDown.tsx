/* Drawn to match ArrowUpRight: same 24px box, same square-cap open stroke, so
   the two marks read as one family. */
export default function ChevronDown() {
  return (
    <svg viewBox="0 0 24 24" aria-hidden="true">
      <path d="m5 9 7 7 7-7" />
    </svg>
  );
}
